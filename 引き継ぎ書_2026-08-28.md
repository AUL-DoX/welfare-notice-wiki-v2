# 引き継ぎ書（2026-08-28 セッション）

前回の[引き継ぎ書_2026-07-17.md](引き継ぎ書_2026-07-17.md)の続き。今回は**WIKIのLLMO（LLM向け最適化）対応**として、`robots.txt`の404解消と`llms.txt`の新規設置を行った。

---

## 1. 経緯：Vercelのログ・分析画面から発覚した問題

福祉通知ウィキ（`wn-wiki.aul-dox.jp`）のVercel分析画面を確認したところ、以下が判明した。

- **バウンスレート88%**、直近7日で訪問者157・PV181と、人間の閲覧というより**クローラー主体のアクセス**に見える傾向
- アクセス元が日本37%・中国22%・カナダ13%・シンガポール13%と、**海外のAI系クローラー（OpenAI・Anthropic・Baidu系など）からのアクセスが疑われる比率**
- Bing（Copilot経由の可能性）から23件のアクセスが確認され、**すでにLLM系クローラーがWIKIを巡回していた**
- ログを見ると `/robots.txt` へのアクセスが**404エラー**になっていた

→ 「クローラーは既に来ているのに、`robots.txt`が存在せずクロールを制御できていない」状態だったため、急ぎ対応した。

---

## 2. やったこと

### 2-1. `robots.ts` の新規作成（[src/app/robots.ts](src/app/robots.ts)）

- Next.js の App Router 規約（既存の`sitemap.ts`と同じ流儀）で `/robots.txt` を動的生成するように変更し、404を解消
- `/api/` `/admin/` はクロール対象から除外
- 主要なLLM／AI検索クローラーを明示的に許可リストに追加：
  `GPTBot`、`ChatGPT-User`、`OAI-SearchBot`（OpenAI系）、`anthropic-ai`、`ClaudeBot`（Anthropic系）、`PerplexityBot`、`Google-Extended`、`Bingbot`
- 既存の `sitemap.ts` へのリンクも記載

### 2-2. `llms.txt` の新規作成（[src/app/llms.txt/route.ts](src/app/llms.txt/route.ts)）

- LLMクローラー向けの新興規格「llms.txt」を `/llms.txt` で配信するAPI Routeを新規作成
- `getDocumentIndex()` から全文書を取得し、カテゴリ別（介護／障がい福祉／共通／未分類）にMarkdown形式の一覧を**動的生成**（既存データを都度読みに行く方式。静的ファイルではないので文書追加時も自動反映される）
- 各文書について「タイトル・URL・要約（120字まで）」を記載し、LLMが一目でサイト構造と主要コンテンツを把握できるようにした
- `Cache-Control: public, max-age=3600, stale-while-revalidate=86400` を設定し、負荷を抑えつつ鮮度も保つようにした

### 2-3. 動作確認

- `npx tsc --noEmit`：エラーなし
- `npm run build`：成功。ルート一覧に `/robots.txt`（○ Static）・`/llms.txt`（ƒ Dynamic）が正しく出力されることを確認済み

```
Route (app)
├ ƒ /llms.txt
├ ○ /robots.txt
```

---

## 3. 未実施（次回以降）

- **ローカル確認のみで、まだ本番（Vercel）へはpush/デプロイしていない。** `git push` してよいかはユーザーの承認待ち。
- **バウンスレート88%の改善策は未着手。** 議論のみ：
  - aul-dox.jp記事群とWIKI間の相互内部リンク強化
  - 各ドキュメントページ冒頭への「1文の定義文」追加（LLMが要約・引用しやすくするため）
- `llms.txt` の内容は自動生成のみで、手動でのキュレーション（「特に重要な文書はこれ」といった強調）はまだ入れていない。要望があれば追加を検討。
- Google Search Console 等への `sitemap.xml` 再送信（robots.txt変更を反映させるため）は未実施。

---

## 4. 過去から持ち越し中の未着手事項（引き続き）

- AUL A×D（`C:\AUL 7Pips-welfare`）の分析JSONをAUL DoX Referenceのwikiに表示する仕組み作り
- v1関連（フォルダ・リポジトリ・Vercelプロジェクト）の最終的な削除タイミング
- 管理者パスワード（`ADMIN_MODE_TOKEN`）の強化（弱いパスワードのまま、提案のみで保留中）
