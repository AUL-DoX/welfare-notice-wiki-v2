# 引き継ぎ書（2026-09-20 セッション）

[行政Wiki 高度検索・差分検知・統計連携基盤20260919.md](引き継ぎ書/行政Wiki%20高度検索・差分検知・統計連携基盤20260919.md)で示した改修方針（DOC_ID・HASH・レキシカル検索強化などのPhase 1）を受け、今回はその出発点として**現行の検索機能の実地検証**と、そこで見つかった不具合の修正、AUL DoX独自の文書識別コードの試作を行った。

作業はブランチ `claude/welfare-notice-db-review-25a7be` 上で実施し、[PR #3](https://github.com/AUL-DoX/welfare-notice-wiki-v2/pull/3)としてpush済み（**未マージ**）。

---

## 1. 経緯：方針のすり合わせ

- DOC_IDは外部の命名規則（URL等）に縛られる必要はなく、**AUL DoX独自のチェックデジット付き識別コード**でよい、という方針を確認
  - 背景：厚労省URLは年度が変わると作成担当者が変更してしまうことが多い一方、表題は踏襲される
- 対象範囲は**介護・障がい福祉のみ**（他分野は扱わない）
- レキシカル検索の精度確保に時間がかかるだろう、という懸念を実地検証で確認 → 実際に複数の穴が見つかった（後述）
- DB化（PostgreSQL）は工数調査のみ実施。現状はJSONファイルベースでも当面問題なく、**書き込み側（管理画面編集をGitに直接コミットする仕組み）の作り直しと日本語全文検索の追加設定が主な工数要因**になるため、Phase 2（週次パイプライン統合）着手時まで見送りと合意

---

## 2. やったこと

### 2-1. 検索マッチングの正規化（[search-normalize.ts](src/lib/search-normalize.ts)）

- 全角/半角の統一（NFKC正規化）を行っていなかったため、`ＥＧ２７`のような全角入力で検索すると0件になる不具合を修正
- 厚労省文書は文字間に半角/全角スペースを挟んで組版されるケースが多く（442件中416件で該当）、これが検索のノイズになっていたため、検索マッチング時のみ横方向の空白を除去するようにした（**表示用の原文は変更していない**）
- メイン検索・返戻対応マニュアル検索・障がい福祉エラーコード検索の3系統すべてに適用

### 2-2. PDFテキスト抽出の文字重複バグ修正（[documents.ts](src/lib/documents.ts)）

- 表紙タイトルなど、縁取り/影効果のために同じ文字を数ピクセルずらして2〜3回重ね描きしているPDFがあり、既存の行判定ロジックがそれを誤って連結し「介 介介護 護護給…」のような重複テキストになっていた
- 同一文字が至近距離（3pt未満）で連続する場合は重ね描きとみなして無視するよう修正し、`npm run index`で索引を再生成（442件成功、758件相当のテキストフィールドが改善）

### 2-3. AUL DoXチェックデジット付き文書ID（[aul-doc-id.ts](src/lib/aul-doc-id.ts)）

- `AD-000001-8`形式のコードを試作。Luhnアルゴリズム（クレジットカード番号等で使われる誤り検出方式）で1桁の誤入力・隣接桁の入れ替えを検出可能
- [assign-doc-ids.ts](scripts/assign-doc-ids.ts)で既存442文書に一括採番し、[data/doc-ids.json](data/doc-ids.json)（slug→docIdマッピング）を生成。再実行しても既存分は変わらない冪等設計
- **UI表示・週次自動収集パイプラインへの組み込みは未着手**（次回以降）

### 2-4. ローカル開発サーバーのポート変更（[.claude/launch.json](.claude/launch.json)）

- メインチェックアウト（`起動.bat`）がポート3000を使用しているため、このworktreeのプレビュー専用ポートを48213に変更し、衝突を回避

### 2-5. 全ページ共通ヘッダーとブランド名変更（[site-header.tsx](src/components/site-header.tsx)）

- 返戻対応マニュアル検索・エラーコード検索・更新情報の各ページにトップページへ戻る導線がなかったため、常時表示（sticky）のヘッダーを追加し、現在地をハイライト表示
- ヘッダーのアクティブ表示色は、当初黒だったものをサイトのアクセントカラーであるamber系に変更（発見性向上の要望を反映）
- サイト名は一時「介護・障がい福祉の行政情報データベース」（AUL Welfare Notice DB）に変更したが、**最終的に元の「介護と障害福祉サービスの通知文Wiki」（AUL Welfare Notice Wiki）に差し戻し**（理由は3節参照）。共通ヘッダー自体は維持

### 2-6. 検索クリアボタンの不具合修正（[page.tsx](src/app/page.tsx)）

- 検索窓がuncontrolledな`<input>`だったため、「検索クリア」リンクのクライアント側遷移で表示上の値がリセットされない不具合を修正（`key={query}`で再マウントさせる方式）
- ボタン表記を「検索を解除」→「検索クリア」に統一

---

## 3. 未実施（次回以降）／スコープに関する結論

- **[PR #3](https://github.com/AUL-DoX/welfare-notice-wiki-v2/pull/3)はまだマージされていない。** レビュー・マージが必要
- AUL DoXチェックデジットコード（`AD-XXXXXX-X`）の
  - 文書詳細ページ等UIへの表示
  - 週次自動収集パイプラインへの組み込み（新規文書追加時の自動採番）

### e-Stat連携（引き継ぎ書20260919のPhase 3）は本サイトでは実施しない

- 統計データの分析結果はすでに`aul-dox.jp`の`data`セクションで公表しており、本Wikiで同じものを重複公表する理由がないため
- 役割分担を明確化：**`aul-dox.jp`（data）＝統計分析・数値の公表**／**本Wiki＝行政文書・通知文の検索データベース**
- したがって、e-Stat APIを使って資料を無理に増やす方向の拡張は行わない

### 資料収集の運用方針

- 今後も**現行の週次自動収集（厚労省・札幌市・WAMNET）＋既存掲載資料の更新反映**のみで様子を見る。無理に資料件数を増やしにいかない
- 上記の結果、**当面やることはレキシカル検索の精度改善**に絞られる（今回対応した全角/半角・字間スペース正規化、PDF抽出バグ修正はその一環）

### DB化（PostgreSQL）

- 今回は着手せず。資料件数を無理に増やさない方針としたため、DB化を急ぐ動機（JSON方式のスケール限界）自体が当面発生しにくくなった
- 着手するとすれば、**書き込み側の設計変更（現行の「管理画面編集→GitHubへ直接コミット」の仕組みをどう扱うか）が必要になったタイミング**（Phase 2：週次パイプラインとの統合・セクション単位差分）
- 安易なDB化は維持コスト（ストレージ・クエリ課金）が資料件数に比例して膨らむリスクがあるため、**急いで移行する理由がない限り現状（JSONファイルベース）を維持**する
- 日本語全文検索（`pg_bigm`等）の要否検討もDB化と合わせて先送り

#### DB化の方向性（2026-09-20時点の調査メモ）

セッション後半で、2026年9月時点のVercel×PostgreSQL連携の成熟度について調査・議論した。DB化に着手する際の出発点として記録しておく。

- **第一候補：Vercel Marketplace経由のNeon PostgreSQL**
  - `DATABASE_URL`等の環境変数がVercel側に自動同期され、コードに接続情報を持たなくてよい
  - Preview DeploymentごとにDBブランチを作れる、Vercel Dashboard上でSQL実行・テーブル編集ができる、scale-to-zero/autoscalingを備える
  - Fluid Compute + `attachDatabasePool()`により、従来言われていた「serverless×SQLはコネクション問題で相性が悪い」という課題がVercel上ではかなり解消されている
- **GCP資産を重視する場合の代替：Vercel + Cloud SQL for PostgreSQL**
  - Vercel Native Integrationではなく、Node.js用Cloud SQL Connector（IAM認証・TLSをConnector側で処理）を介した一般的な外部DB接続になる
  - Public IP直結ではなくConnector経由にする（Vercelは送信元IPが動的なため）
- **移行の段取り案（Google Sheets運用からの自然な延長）**

  ```text
  Google Sheets（収集・確認・HITL）
     ↓
  PostgreSQL（Neon等・本番DB）
     ↓
  公開サイト（Vercel）
  ```

  前回合意した「スプレッドシート運用→限界が来たらDB」の方針とも整合する。Sheetsを恒久的な人手チェック層として残し、SQLを確定データの本番DBとして位置づけると、既存の週次自動収集パイプライン（自動収集→人の目でチェック→公開）の性質にも合う。
- 結論として、DB化に着手するときは**Neon PostgreSQLで小さく試す**のが合理的、という整理に至った。ただし着手時期自体は変わらずPhase 2（週次パイプライン統合）以降。

---

## 4. 過去から持ち越し中の未着手事項（[引き継ぎ書_2026-08-28.md](引き継ぎ書_2026-08-28.md)より引き続き）

- AUL A×D（`C:\AUL 7Pips-welfare`）の分析JSONをAUL DoX Referenceのwikiに表示する仕組み作り
- v1関連（フォルダ・リポジトリ・Vercelプロジェクト）の最終的な削除タイミング
- 管理者パスワード（`ADMIN_MODE_TOKEN`）の強化（弱いパスワードのまま、提案のみで保留中）
- バウンスレート88%の改善策（内部リンク強化、ドキュメントページ冒頭への定義文追加など）
- Google Search Console等への`sitemap.xml`再送信
