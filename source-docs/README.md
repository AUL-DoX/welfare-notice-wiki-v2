# source-docs

このフォルダに `.pdf` `.pptx` `.md` `.txt` を置くと、トップページ表示時に自動で走査して一覧化します。

## 想定運用

1. 札幌市や厚生労働省の通知文をこのフォルダに入れる
2. `npm run dev` でアプリを開く
3. 一覧ページで検索・詳細確認をする

## 補足

- OCRは入れていません
- 画像PDFは対象外です
- PowerPointはスライド本文を抽出します
- 抽出精度は原資料のレイアウトに影響されます
